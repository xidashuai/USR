import { test, expect } from 'vitest'
import { V2D, Vector2D } from '../utils/vector'

test('asfd', () => {
  const point = [
    { x: 120, y: 340 },
    { x: 220, y: 240 },
    { x: 320, y: 140 },
    { x: 420, y: 240 }
  ]
})
